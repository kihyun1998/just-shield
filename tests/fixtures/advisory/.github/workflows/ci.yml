name: CI
on: push
permissions:
  contents: read
jobs:
  b:
    runs-on: ubuntu-latest
    steps:
      # 실제 권고(CVE-2025-30066)에 등재된 악성 커밋 — 동봉 DB가 잡아야 한다
      - uses: tj-actions/changed-files@0e58ed8671d6b60d0890c21b07f8835ace038e67
      - uses: tj-actions/changed-files@0123456789abcdef0123456789abcdef01234567 # 등재 안 된 SHA
      - run: cargo test
